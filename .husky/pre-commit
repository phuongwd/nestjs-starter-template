#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run TypeScript compiler check
npm run type-check

# Run module dependency check
ts-node src/utils/module-checker.ts

# Run linting
npm run lint

# Run tests
npm test 